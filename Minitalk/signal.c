#include "ft_minitalk.h"
int main(void)
{
    // int pid = fork();
    // if(pid == -1)
        // return -1;
    // if(pid == 0)
    // {
        // printf(" I am here in a child process and the PID is [%d]\n", getpid());
        // while (1)
        // {
            // printf("I am going to run for infinite amount of time\n");
            //  usleep(1000);
        // }
    // }
    // else
    // {
        // sleep(1);
        // kill(pid, SIGKILL);
        // int status;
        // int terminated = wait(&status);
        // printf("The terminated child process is %d \n",terminated);
    // }
    
}